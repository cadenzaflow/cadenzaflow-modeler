diff --git a/node_modules/karma/lib/file-list.js b/node_modules/karma/lib/file-list.js
index b4ec01d..c8d6a76 100644
--- a/node_modules/karma/lib/file-list.js
+++ b/node_modules/karma/lib/file-list.js
@@ -1,7 +1,9 @@
 'use strict'
 
 const { promisify } = require('util')
-const mm = require('minimatch')
+// minimatch 9+ moved its function to a named export; fall back to default for older versions.
+const _mm = require('minimatch')
+const mm = typeof _mm === 'function' ? _mm : _mm.minimatch
 const Glob = require('glob').Glob
 const fs = require('graceful-fs')
 const statAsync = promisify(fs.stat.bind(fs))
@@ -68,9 +70,10 @@ class FileList {
           return
         }
 
-        const mg = new Glob(pathLib.normalize(pattern), { cwd: '/', follow: true, nodir: true, sync: true })
+        // glob 10+ removed `sync: true` and `.found`/`.statCache`; use globSync + fs.statSync.
+        const matchedPaths = require('glob').globSync(pathLib.normalize(pattern), { cwd: '/', follow: true, nodir: true })
 
-        const files = mg.found
+        const files = matchedPaths
           .filter((path) => {
             if (this._findExcluded(path)) {
               log.debug(`Excluded file "${path}"`)
@@ -82,7 +85,7 @@ class FileList {
               return true
             }
           })
-          .map((path) => new File(path, mg.statCache[path].mtime, nocache, type, isBinary))
+          .map((path) => new File(path, require('fs').statSync(path).mtime, nocache, type, isBinary))
 
         if (nocache) {
           log.debug(`Not preprocessing "${pattern}" due to nocache`)
@@ -92,7 +95,7 @@ class FileList {
 
         this.buckets.set(pattern, files)
 
-        if (_.isEmpty(mg.found)) {
+        if (_.isEmpty(matchedPaths)) {
           log.warn(`Pattern "${pattern}" does not match any file.`)
         } else if (_.isEmpty(files)) {
           log.warn(`All files matched by "${pattern}" were excluded or matched by prior matchers.`)
diff --git a/node_modules/karma/lib/helper.js b/node_modules/karma/lib/helper.js
index 9be1193..57daf64 100644
--- a/node_modules/karma/lib/helper.js
+++ b/node_modules/karma/lib/helper.js
@@ -3,7 +3,9 @@
 const _ = require('lodash')
 const mkdirp = require('mkdirp')
 const useragent = require('ua-parser-js')
-const mm = require('minimatch')
+// minimatch 9+ moved its function to a named export; fall back to default for older versions.
+const _mm = require('minimatch')
+const mm = typeof _mm === 'function' ? _mm : _mm.minimatch
 
 exports.browserFullNameToShort = (fullName) => {
   const ua = useragent(fullName)
diff --git a/node_modules/karma/lib/init.js b/node_modules/karma/lib/init.js
index f55f94e..77c631a 100755
--- a/node_modules/karma/lib/init.js
+++ b/node_modules/karma/lib/init.js
@@ -3,7 +3,9 @@
 const readline = require('readline')
 const path = require('path')
 const glob = require('glob')
-const mm = require('minimatch')
+// minimatch 9+ moved its function to a named export; fall back to default for older versions.
+const _mm = require('minimatch')
+const mm = typeof _mm === 'function' ? _mm : _mm.minimatch
 const exec = require('child_process').exec
 
 const helper = require('./helper')
diff --git a/node_modules/karma/lib/preprocessor.js b/node_modules/karma/lib/preprocessor.js
index b6bf756..80718c8 100644
--- a/node_modules/karma/lib/preprocessor.js
+++ b/node_modules/karma/lib/preprocessor.js
@@ -20,7 +20,9 @@ const tryToRead = function (path, log) {
   })
 }
 
-const mm = require('minimatch')
+// minimatch 9+ moved its function to a named export; fall back to default for older versions.
+const _mm = require('minimatch')
+const mm = typeof _mm === 'function' ? _mm : _mm.minimatch
 const { isBinaryFile } = require('isbinaryfile')
 const _ = require('lodash')
 const CryptoUtils = require('./utils/crypto-utils')
diff --git a/node_modules/karma/lib/watcher.js b/node_modules/karma/lib/watcher.js
index 7a16667..16527f9 100644
--- a/node_modules/karma/lib/watcher.js
+++ b/node_modules/karma/lib/watcher.js
@@ -1,6 +1,8 @@
 'use strict'
 
-const mm = require('minimatch')
+// minimatch 9+ moved its function to a named export; fall back to default for older versions.
+const _mm = require('minimatch')
+const mm = typeof _mm === 'function' ? _mm : _mm.minimatch
 const braces = require('braces')
 const PatternUtils = require('./utils/pattern-utils')
 
